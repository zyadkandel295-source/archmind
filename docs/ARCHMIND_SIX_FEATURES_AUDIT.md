# ArchMind Six-Feature Current-State Audit

Audit date: 2026-07-12. Evidence is the current working tree, including staged deletions and unstaged changes. The runtime is Next.js 14, Express/TypeScript, an active JSON-backed `MemoryStore`, PostgreSQL migration definitions, BullMQ hooks, Firebase/JWT authentication, OpenRouter, and Notion/Google adapters. The prior Electron desktop files are staged for deletion and are therefore not an active capability.

| Feature | Existing status | Relevant files | What works | What is mocked or broken | Missing work | Security concerns | Planned implementation |
|---|---|---|---|---|---|---|---|
| Zero-code setup | UI only | `apps/web/components/automation-client.tsx`, `apps/api/src/services/execution-engine.ts` | Browser infers steps and calls the execution bridge. | Browser parsing is heuristic and unsaved; LLM output goes directly toward execution; no workflow versions. | Deterministic proposal, persisted versions, validation, lifecycle and run history. | Client can present an interpretation not enforced by backend. | Backend proposal/validation and versioned workflow APIs, then bind the editor to them. |
| Trust and transparency | Partially implemented | `execution-engine.ts`, `execution-bridge.ts`, `memory.ts` | A few hard-coded tool cases pause for approval; logs persist in the JSON store. | Approval ownership is not checked in the legacy resume service; risk list is hard-coded; audit is mutable; rollback text overclaims. | Central policy, scoped grants, append-only audit, real undo, pause controls. | Public unsigned webhook can run an assistant; parameters may be exposed; client cannot be authoritative. | Central risk registry and authorization service; hash-chained audit plus DB immutability trigger. |
| Real system-level actions | Mocked | `tool-gateway.ts`, `execution-engine.ts`, `queue.ts`, `worker.ts` | Real Google/Notion code paths exist when configured; bounded retry exists. | Several adapters use mock environment values; audit-sheet write is simulated/forced; worker only returns `indexed: true`; no active local runtime. | Controlled local action executor, scoped paths, durable workflow queue/watchers. | LLM chooses gateway method names; public webhook lacks signatures/replay protection. | Controlled registry and canonical path enforcement; disable unavailable adapters honestly. |
| Compounding personal memory | Missing | `memory.ts`, conversations and RAG source records | Conversation records and assistant-scoped source retrieval exist. | “MemoryStore” is a general persistence class, not personal memory; no memory policy or controls. | Scoped memory records, provenance, visibility, deletion/export/settings and retrieval filtering. | Cross-scope leakage if transcript/RAG is mistaken for durable memory. | Owner-first memory CRUD with scope and assistant visibility. |
| Branded packaging and resale/licensing | Missing | `billing.ts`, public assistant routes | Public assistant pages and billing stubs exist. | No immutable package, entitlement, acquisition, license, listing or safe export. | Package/version/entitlement services and creator/library UI. | Private data and secrets could leak without a manifest allowlist. | Immutable safe manifest versions and server-authoritative entitlements; paid checkout disabled without provider. |
| Downloadable standalone desktop assistants | Broken | staged deletions under `apps/desktop`, deleted desktop API/installer modules | Historical code and installer scratch artifacts exist. | Desktop workspace, installer service, routes and tests are staged for deletion; scratch EXEs are not release evidence. | Restore/replace runtime, secure build pipeline, signing, artifact validation and OS tests. | Old implementation embedded or distributed artifacts without current verification; scratch downloads must not be trusted. | Implement secure bootstrap/device authorization now; keep builds disabled until an intentional signed runtime exists. |

## Shared foundations

| Foundation | Existing status | Relevant files | What works | What is mocked or broken | Missing work | Security concerns | Planned implementation |
|---|---|---|---|---|---|---|---|
| Authentication | Partially implemented | `middleware/auth.ts`, `modules/auth.ts`, Firebase services | JWT and Firebase token paths; protected routes. | README and browser token/session behavior are inconsistent. | Production cookie/CSRF decision and session revocation. | Bearer tokens in browser storage increase XSS impact. | Retain auth, enforce server authorization on every new resource. |
| User and organization ownership | Partially implemented | assistant store, migrations 001–004 | User-owned assistants and cross-user tests. | No organization model. | Add organization only when product semantics are defined. | Mixed `app.user_id` vs `app.current_user_id` RLS setting names. | New records carry owner; use one RLS helper. |
| Database schema | Partially implemented | `db/migrations`, `memory.ts` | Additive migrations and RLS on early tables. | Active app uses JSON memory store instead of PostgreSQL. | Repository/database adapter and migration runner. | Runtime persistence lacks DB transactional and append-only guarantees. | Add migration now; retain JSON compatibility for current tests/dev. |
| Assistant isolation | Partially implemented | assistants/chat/sources routes and tests | Ownership filtering and assistant conversation/source tests pass. | Legacy bridge approval lookup is not owner-scoped. | Cross-user tests for every new entity. | Object-ID guessing against unscoped methods. | Owner-filter service methods and 404 non-disclosure. |
| Permission model | Missing | legacy bridge approval records | One-off approval for three cases. | No scoped grants or revocation. | Full grant modes and policy evaluation. | Backend bypass possible in legacy tool gateway. | Central permission grants and policy gate. |
| Secrets management | Partially implemented | `.env.example`, config, notion crypto | Backend-only OpenRouter and encrypted Notion adapter path. | `.env.example` contains `mock-*` token strings. | Production secret inventory/rotation. | Mock-looking values can accidentally enable “connected” status. | Empty examples; reject placeholder credentials. |
| Background jobs | Mocked | `queue.ts`, `worker.ts` | BullMQ enqueue/retry shell. | Worker reports success without ingestion. | Real handlers, cancellation, health, backpressure. | False success and duplicate jobs. | Idempotency plus real handler registration. |
| Notifications | Missing | automation UI | UI toasts only. | No persistent notification service. | User/device notification records. | Sensitive details in notifications. | Redacted notification adapter. |
| Audit logging | Partially implemented | bridge logs, Notion logs | Some persistent logs. | Mutable and incomplete. | Append-only security events, trace IDs and redaction. | Tampering and sensitive output retention. | Hash chain plus PostgreSQL mutation-blocking trigger. |
| Billing, entitlements, licensing | Mocked | `billing.ts` | Plan metadata. | Billing is a stub; no licenses. | Entitlement abstraction and provider webhooks. | Fake payment success must be impossible. | Free/private acquisition only until provider is configured. |
| File storage | Partially implemented | sources module, local storage, S3 env | Validated uploads and 15 MB limit; traversal tests. | Local dev storage; S3 not active. | Object-store adapter and retention. | Local production files and parser attack surface. | Scoped paths, content limits, storage docs. |
| AI-provider integration | Partially implemented | LLM/AI services | OpenRouter routing and clean errors. | Workflow planning trusts LLM JSON; no cost ledger. | Schema-bound proposal assistance and budgets. | Prompt injection into tool planning. | Deterministic workflow validation; model never executes proposal. |
| Rate limits | Implemented but unverified | `middleware/rate-limit.ts` | Global middleware. | No route-specific quotas. | Workflow/build/webhook quotas. | Expensive endpoints can share a broad limit. | Add per-operation idempotency and bounded limits. |
| Error monitoring | Missing | error middleware, Sentry env | Structured HTTP error shape. | Sentry is configuration only. | Provider initialization and trace correlation. | Stack/error leakage must remain disabled. | Document hook and production requirement. |
| Analytics | Partially implemented | analytics routes, Firebase activity | Assistant analytics and site activity endpoint. | Site activity endpoint returns success without persistence. | Consent/privacy controls. | Accidental PII collection. | Minimize metadata and document flows. |
| Automated tests | Partially implemented | `apps/api/tests/routes.test.ts` | 31 API tests passed on audit date. | No six-feature, E2E, desktop or build authorization suite. | Unit/integration/security tests for new domains. | Critical authorization unproved. | Add API integration tests and unit policy/path tests. |
| Production deployment | Partially implemented | Dockerfiles, Railway/AWS/Kong starters | Build/deploy starters and health endpoint. | No complete runbook/readiness or migration command. | Domain, TLS, worker, storage, backups, rollback docs. | Health leaks configuration booleans; insecure defaults. | Harden health/readiness and document production setup. |
| Desktop release process | Broken | staged desktop deletions, scratch installers | No active release pipeline. | Scratch artifacts and prior code are not trustworthy releases. | Intentional runtime, signing, CI, checksums and validation. | Installer tampering and credential embedding. | Keep build endpoint explicitly unavailable; document requirements. |

## Baseline verification

- `npm test -- --reporter=verbose`: 31/31 tests passed (the npm reporter flag was ignored; Vitest still ran normally).
- Initial combined test/typecheck/build command exceeded its wrapper timeout before emitting results; each command must be recorded independently.
- No desktop build is possible from the current working tree because `apps/desktop` is staged for deletion.

## Architecture decision

The current JSON-backed store remains active for compatibility, but the new domain is represented by an additive PostgreSQL migration with ownership indexes and RLS. The implementation must not claim production durability until the API is wired to PostgreSQL transactions. The legacy execution bridge remains isolated; new workflows use the centralized platform policy service.
